(function () {
    'use strict';

    angular
        .module('app')
        .controller('bpmCalculatorController', bpmCalculatorController);
	
	bpmCalculatorController.$inject = ['$scope','$rootScope'];
    function bpmCalculatorController($scope,$rootScope) {
	
		var init = function() {
			//$scope.map.setCenter(new google.maps.LatLng(response.data.lat,response.data.long));
			//$scope.map.setZoom(15);
		}
		
		/********************* Map Section ***********************************/
			
	
    }
    

})();

